# === BẢO MẬT - Không bao giờ commit ===
# *.json thu hẹp còn tên file key thật (thay vì chặn MỌI json trong repo —
# sẽ vô tình nuốt config json hợp lệ thêm sau này, vd game_config json mới).
.env
shared_keys/
*-key.json
*-dbt.json
*service-account*.json

# === Airflow tự sinh ra - không cần commit ===
airflow_platform/logs/
airflow_platform/plugins/

# === dbt tự sinh ra ===
dbt_core/target/
dbt_core/dbt_packages/
dbt_core/logs/

# === Hệ thống ===
__pycache__/
.DS_Store

target/
dbt_packages/
logs/